diff --git a/src/com/bkeryah/managedBean/investment/ContractDirectBean.java b/src/com/bkeryah/managedBean/investment/ContractDirectBean.java
--- a/src/com/bkeryah/managedBean/investment/ContractDirectBean.java
+++ b/src/com/bkeryah/managedBean/investment/ContractDirectBean.java
@@ -692,9 +692,9 @@
 	private Integer feesDiscountDuration(ContractsFees fees, Integer discountStatus, Integer discountRemainder,
 			int count) {
 		// if (discountStatus == 1) {
-		// if (contractType == 1 && (count <= 1 || discountDuration > 365)) {
-		// fees.setDiscountExceptionel(365);
-		// discountRemainder = discountDuration - 365;
+		// if (contractType == 1 && (count <= 1 || discountDuration > 360)) {
+		// fees.setDiscountExceptionel(360);
+		// discountRemainder = discountDuration - 360;
 		//
 		// } else if (contractType == 2 && (count <= 1 || discountDuration >
 		// 30)) {
@@ -703,5 +703,5 @@
 		// } else
 		// fees.setDiscountduration(discountRemainder);
 
-		if (contractType == 1 && (count < 1 || discountDuration > 365)) {
+		if (contractType == 1 && (count < 1 || discountDuration > 360)) {
 			if (discountStatus == 1) {
@@ -707,6 +707,6 @@
 			if (discountStatus == 1) {
-				fees.setDiscountduration(365);
-				discountRemainder = discountDuration - 365;
+				fees.setDiscountduration(360);
+				discountRemainder = discountDuration - 360;
 			}
 			if (discountStatus == 0) {
 				fees.setDiscountduration(discountRemainder);
@@ -835,8 +835,8 @@
 
 			switch (contractType) {
 			case 1:
-				totalDiscount = ((contractDirect.getAnnualRent() / 365) * discountDuration) + discountExceptionel;
-				reminderDiscountAmount = ((contractDirect.getAnnualRent() / 365)
+				totalDiscount = ((contractDirect.getAnnualRent() / 360) * discountDuration) + discountExceptionel;
+				reminderDiscountAmount = ((contractDirect.getAnnualRent() / 360)
 						* (reminderDiscountDuration - discountDuration));
 				break;
 			case 2:
@@ -953,7 +953,7 @@
 		}
 	}
 
-	public void oadSuCategoryByMainCategoryId() {
+	public void loadSuCategoryByMainCategoryId() {
 		if (contractMaincatgId != null && contractMaincatgId > 0) {
 			contractDirect.setContractMaincatgId(contractMaincatgId);
 		}
@@ -1032,7 +1032,7 @@
 			dataAccessService.save(subCategory);
 			// contractSubcategoryList =
 			// dataAccessService.loadAllContractSubcategory();
-			//loadSuCategoryByMainCategoryId();
+			loadSuCategoryByMainCategoryId();
 			MsgEntry.addAcceptFlashInfoMessage(Utils.loadMessagesFromFile("success.operation"));
 
 		} catch (
diff --git a/src/utilities/HijriCalendarUtil.java b/src/utilities/HijriCalendarUtil.java
--- a/src/utilities/HijriCalendarUtil.java
+++ b/src/utilities/HijriCalendarUtil.java
@@ -795,6 +795,14 @@
 		yearsList.add(new YearClass(1460, 29, 29, 30, 29, 30, 29, 30, 30, 29, 30, 30, 29, "2029/05/16"));
 		yearsList.add(new YearClass(1461, 29, 29, 30, 29, 30, 29, 30, 30, 29, 30, 30, 29, "2029/05/16"));
 		yearsList.add(new YearClass(1462, 29, 29, 30, 29, 30, 29, 30, 30, 29, 30, 30, 29, "2029/05/16"));
+		yearsList.add(new YearClass(1463, 29, 30, 29, 30, 29, 30, 29, 30, 30, 29, 30, 30, "2041/01/04"));
+		yearsList.add(new YearClass(1464, 29, 30, 29, 29, 30, 29, 29, 30, 30, 29, 30, 30, "2041/12/25"));
+		yearsList.add(new YearClass(1465, 30, 29, 30, 29, 29, 30,  29, 29, 30, 29, 30, 30, "2042/12/14"));		
+		yearsList.add(new YearClass(1466, 30, 29, 30, 30, 29, 29,  30, 29, 29,  30, 29, 30, "2043/12/03"));
+		yearsList.add(new YearClass(1467, 30, 29, 30, 30, 30, 29, 29, 30, 29, 29, 30, 29, "2044/11/21"));		
+		yearsList.add(new YearClass(1468, 30, 29, 30, 30, 30, 29, 30, 29, 30, 29, 29, 30, "2045/11/10"));		
+		yearsList.add(new YearClass(1469, 29, 30, 29, 30, 30, 29, 30, 30, 29,  30, 29, 29, "2046/10/31"));
+		yearsList.add(new YearClass(1470, 30, 29, 30, 29, 30, 29, 30, 30, 29, 30,  29, 30, "2047/10/20"));
 		return yearsList;
 
 	}
